
# Development environment variables (do NOT use in production)
# Copy this file to a local file and keep it out of version control if necessary.
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123
POSTGRES_DB=sfam

# MinIO credentials (dev-only)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Optionally override Kafka host listener for host access
# KAFKA_HOST=localhost
